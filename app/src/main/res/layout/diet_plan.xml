<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Title for the Diet Plan -->
    <TextView
        android:id="@+id/dietPlanTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Diet Plan"
        android:textSize="24sp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="16dp" />

    <!-- Description of the Diet Plan -->
    <TextView
        android:id="@+id/dietPlanDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your personalized diet plan will appear here."
        android:textSize="16sp"
        android:fontFamily="sans-serif"
        android:layout_marginBottom="24dp" />

    <!-- RecyclerView to display the list of diet plans -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/dietPlanRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"/>

    <!-- Button to edit the diet plan -->
    <Button
        android:id="@+id/editDietPlanButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Edit Diet Plan"
        android:backgroundTint="?colorPrimary"
        android:textColor="?colorOnPrimary" />
</LinearLayout>

