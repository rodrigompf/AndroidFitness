<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/imcCalculatorTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="IMC Calculator"
        android:textSize="24sp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="16dp" />

    <EditText
        android:id="@+id/weightInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your weight (kg)"
        android:inputType="numberDecimal"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/heightInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your height (m)"
        android:inputType="numberDecimal"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/calculateImcButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Calculate IMC"
        android:backgroundTint="?colorPrimary"
        android:textColor="?colorOnPrimary"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/imcResultText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your IMC will appear here."
        android:textSize="16sp"
        android:fontFamily="sans-serif"
        android:layout_marginTop="16dp" />
</LinearLayout>
